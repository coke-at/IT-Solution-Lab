(()=>{const dlg=document.querySelector('#knowledgeDialog'),body=document.querySelector('#knowledgeBody');let mode='delivery';const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function shell(){body.innerHTML=`<div class="ops-head"><div><small>${mode==='delivery'?'DELIVERY STANDARD':'TROUBLESHOOTING'}</small><h2>${mode==='delivery'?'设备交付标准':'设备问题排障'}</h2></div></div><div class="knowledge-search"><span>⌕</span><input id="kSearch" placeholder="搜索厂商、设备名称、型号或关键词"><button id="kGo">查询</button></div><div id="kResults" class="knowledge-results"><div class="ops-empty">输入设备名称或型号，例如 S5735、AF、OceanStor、R4900。</div></div>`;document.querySelector('#kGo').onclick=search;document.querySelector('#kSearch').onkeydown=e=>e.key==='Enter'&&search();document.querySelector('#kSearch').focus()}function search(){const q=document.querySelector('#kSearch').value.trim().toLowerCase(),box=document.querySelector('#kResults');if(!q)return;const rows=products.filter(p=>[p.vendor,p.name,p.model,p.type,p.keys.join(' '),p.aliases].join(' ').toLowerCase().includes(q)).slice(0,12);box.innerHTML=rows.length?rows.map((p,i)=>`<button data-k="${products.indexOf(p)}"><span class="type" style="--tc:${typeColors[p.type]||'#ddd'}">${p.type}</span><b>${esc(p.vendor)} · ${esc(p.name)}</b><small>${esc(p.model)}</small><i>查看${mode==='delivery'?'交付标准':'排障手册'} →</i></button>`).join(''):`<div class="ops-empty">没有匹配设备，请尝试系列简称或厂商名称。</div>`;box.querySelectorAll('[data-k]').forEach(b=>b.onclick=()=>show(products[+b.dataset.k]))}function show(p){const b=getBook(p);body.innerHTML=`<button class="back-k" id="kBack">← 返回搜索</button><div class="k-device"><span class="type" style="--tc:${typeColors[p.type]||'#ddd'}">${p.type}</span><small>${esc(p.vendor)}</small><h2>${esc(p.name)}</h2><p>${esc(p.model)}</p></div>${mode==='delivery'?`<div class="k-standard"><h3>交付目标</h3><p>${esc(b.does)}</p><h3>标准实施步骤</h3><ol>${b.deploy.map((x,i)=>`<li><b>${String(i+1).padStart(2,'0')}</b><span>${esc(x)}</span></li>`).join('')}</ol><h3>交付注意事项</h3><ul><li>实施前核对具体型号、版本、License、兼容清单和网络拓扑。</li><li>所有配置变更必须先备份，并明确维护窗口、影响范围和回退条件。</li><li>不得只验证“设备在线”，应完成业务流、性能、告警和冗余切换测试。</li><li>移交拓扑图、地址表、账号表、配置备份、测试记录、授权与维保信息。</li></ul><h3>验收清单</h3><div class="checklist">${['设备型号与数量一致','基础配置与安全基线完成','业务功能验证通过','高可用/故障切换通过','监控告警已接入','配置与文档完成移交'].map(x=>`<label><input type="checkbox">${x}</label>`).join('')}</div></div>`:`<div class="k-standard"><h3>先做故障定界</h3><p>确认发生时间、影响用户、业务范围、是否持续、最近变更和可回退点。先取证，后变更。</p><h3>推荐排查顺序</h3><ol><li><b>01</b><span>检查电源、链路、接口、资源与依赖服务。</span></li><li><b>02</b><span>检查告警、日志、会话、路由、表项或系统状态。</span></li><li><b>03</b><span>对比正常基线与最近变更，提出可验证假设。</span></li><li><b>04</b><span>使用最小影响操作验证，必要时执行回退。</span></li></ol><h3>常见问题</h3><div class="trouble-list">${b.trouble.map(([x,y])=>`<article><b>${esc(x)}</b><p>${esc(y)}</p></article>`).join('')}</div><h3>升级厂商前准备</h3><ul><li>序列号、型号、版本、拓扑和问题发生时间。</li><li>完整日志、诊断包、告警截图、关键配置和复现步骤。</li><li>影响范围、临时绕行措施、已尝试操作及结果。</li></ul></div>`}<p class="detail-note">内容为该设备类别的工程方法，具体命令与界面路径以对应型号和版本手册为准。</p>`;document.querySelector('#kBack').onclick=shell}document.querySelectorAll('[data-knowledge]').forEach(b=>b.onclick=()=>{mode=b.dataset.knowledge;shell();dlg.showModal()});document.querySelector('#knowledgeClose').onclick=()=>dlg.close();dlg.onclick=e=>{if(e.target===dlg)dlg.close()}})();
